import { Injectable } from "@angular/core";

@Injectable({ providedIn: "root" })
export class SettingsService {
  // Centraliza as preferências da aplicação quando a tela de configurações for implementada.
}
